fn factorial(n) {
    if (n <= 1) {
        return 1
    }
    return n * factorial(n - 1)
}

fn main() {
    println("Factorial examples:")
    println(factorial(1))
    println(factorial(5))
    println(factorial(10))
}

main()
