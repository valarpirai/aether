fn fact(n) {
    print("fact called with: ")
    println(n)
    if (n <= 1) {
        return 1
    }
    return n * fact(n - 1)
}

println("Starting...")
let result = fact(3)
println("Result:")
println(result)

fn main() {}
